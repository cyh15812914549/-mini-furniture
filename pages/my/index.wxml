<!--pages/my/index.wxml-->
<view>
<!--    <block wx:for="{{imgUrls}}" wx:key="index">-->
<!--       <image src="{{item}}" class="image1" data-src="{{item}}" catchtap="previewImages"></image>-->
<!--    </block>-->
    <block wx:for="{{imgUrls}}" wx:key="key">
        <image src="{{item}}" style="width: 100%;height: 350px;display:block;"  data-src="{{item}}" catchtap="previewImages"></image>
    </block>
</view>
